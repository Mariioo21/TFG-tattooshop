spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tattoo_shop?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  sql:
    init:
      mode: always

jwt:
  secret: mi_secreto_super_seguro_123456
  expiration: 86400000 # 24 horas

logging:
  level:
    org.springframework.security: DEBUG